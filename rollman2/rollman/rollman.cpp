#include <bits/stdc++.h>
using namespace std;

int main(void){
	int X, Y, Z;
	cin >> X >> Y >> Z;

	int P;
	cin >> P;

	vector< vector< vector< int > > S(X, vector< vector<int> >(Y, vector<int>(Z, 0) ) );
	return 0;
}
